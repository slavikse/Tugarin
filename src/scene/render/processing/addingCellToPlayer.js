// todo добавлять относительно координат ячейки игрока
export default function addingCellToPlayer({ playerCell, cell }) {
  // console.log(playerCell, cell);

  // let cell;

  // if (isTopSide) {
  //   console.log('добавить наверх');
  //   cell = {
  //     x: playerCell.x,
  //     y: playerCell.y + state.cell.height,
  //   };
  // } else if (isRightSide) {
  //   console.log('добавить вправо');
  //   cell = {
  //     x: playerCell.x - state.cell.width,
  //     y: playerCell.y,
  //   };
  // } else if (isBottomSide) {
  //   console.log('добавить вниз');
  //   cell = {
  //     x: playerCell.x,
  //     y: playerCell.y - state.cell.height,
  //   };
  // } else if (isLeftSide) {
  //   console.log('добавить влево');
  //   cell = {
  //     x: playerCell.x + state.cell.width,
  //     y: playerCell.y,
  //   };
  // }

  // if (cell) {
  // const { type, color } = playerCell;
  // state.player.cells.push({ ...cell, type, color });
}
